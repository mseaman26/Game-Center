{
  "version": 2,
  "builds": [
    { "src": "/api/(.*)", "dest": "/serverless.js" },
    { "src": "serverless.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/serverless.js" }
  ],
  "build": {
    "env": {
      "CI": "false"
    }
  }
}
